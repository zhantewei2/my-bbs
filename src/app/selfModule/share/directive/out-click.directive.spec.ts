import { OutClickDirective } from './out-click.directive';

describe('OutClickDirective', () => {
  it('should create an instance', () => {
    const directive = new OutClickDirective();
    expect(directive).toBeTruthy();
  });
});
