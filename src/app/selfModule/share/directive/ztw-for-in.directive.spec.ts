import { ZtwForInDirective } from './ztw-for-in.directive';

describe('ZtwForInDirective', () => {
  it('should create an instance', () => {
    const directive = new ZtwForInDirective();
    expect(directive).toBeTruthy();
  });
});
