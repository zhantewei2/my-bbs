import { InnerTemplateDirective } from './inner-template.directive';

describe('InnerTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new InnerTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
