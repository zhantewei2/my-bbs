import { RowsValidatorDirective } from './rows-validator.directive';

describe('RowsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RowsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
