import { PreventPressSpaceDirective } from './prevent-press-space.directive';

describe('PreventPressSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventPressSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
