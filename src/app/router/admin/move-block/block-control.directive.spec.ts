import { BlockCOntrolDirective } from './block-control.directive';

describe('BlockCOntrolDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockCOntrolDirective();
    expect(directive).toBeTruthy();
  });
});
